Headers are used in the app where image is shown at the top of the screen e.g. Welcome screen.

The format of the filename should be 'header1.png'

The app then randomly chooses a number at startup and gets the header image whose name matches 'header' + randomNumber + '.png'